name: CD - Promoción y Despliegue de Modelo

on:
  workflow_dispatch:

jobs:
  deploy-model:
    runs-on: ubuntu-latest
    
    steps:
      - name: Clonar el repositorio
        uses: actions/checkout@v3
      
      - name: Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      
      - name: Instalar dependencias
        run: |
          pip install -r requirements.txt
      
      - name: Entrenar modelo (para despliegue)
        run: |
          mkdir -p mlruns
          python src/train.py
      
      - name: Validar modelo antes de despliegue
        run: |
          python src/validate.py
      
      - name: Promocionar modelo
        run: |
          echo "✅ Modelo validado y listo para producción"
          echo "📦 Artefactos disponibles en mlruns/"
          echo "🚀 En un escenario real, aquí se desplegaría con:"
          echo "   mlflow models serve -m mlruns/.../model -p 5000"